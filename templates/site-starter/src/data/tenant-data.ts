/**
 * Tenant data — the generator OVERWRITES this file with per-tenant content.
 *
 * The default ships re-exporting MOCK_DATA so the pristine template still
 * builds without any generation step (mock-data parity).
 */

export { MOCK_DATA as TENANT_DATA } from './mock-data.js';
